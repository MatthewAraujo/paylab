# B8 — Validate the integrated workflow and document operation

## Objective

Update operating documentation, regenerate OpenAPI for the console, run all gates, and record the first native full Benchmark Run.

## Acceptance Criteria Served

US-1, US-64, US-65 (and end-to-end validation of all others).

## Affected Files / Areas

- `PROJECT.md` (stable commands and capability only), `docs/benchmark.md`, `docs/DEVELOPMENT.md`, `.env.example`.
- OpenAPI output consumed by `paylab-ui` (`openapi/paylab.json` sync is a UI task).
- First generated native Summary and local Artifacts.

## Test-First Plan

- Documentation check: every command in the docs exists in `package.json`.
- Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:integration`, `pnpm test:e2e`, `pnpm build`.
- Verify a production-mode build serves no benchmark route.
- One complete manual Run from a clean committed revision: verify progress, terminal Summary, Git changes, Artifacts outside Git, sanitized logs, and comparison with imported history where compatible.

## Implementation Notes

- Keep decisions in `CONTEXT.md` and ADRs; `PROJECT.md` gets only pointers.
- Do not claim full success unless the expensive suite actually completed.

## Dependencies

B1–B7.

## Completion Signal

All automated gates pass, production exposes nothing, one native full Run is reviewed, and the docs are current.
