# T15 — Project handbook, CI and developer documentation

## Objective

Rewrite `PROJECT.md` for PayLab, finish CI and environment documentation, and make sure the repository is usable by a newcomer.

## Acceptance Criteria Served

US-80, US-81, US-84.

## Affected Files / Areas

- `PROJECT.md` (stable handbook, replacing the reference project's), env example files, CI workflows, a short developer guide including the Docker requirement, the pull request template if it mentions removed concepts.
- `CONTEXT.md` gets only pointers if needed; no duplication with `PROJECT.md`.

## Test-First Plan

Documentation and pipeline validation:
- Following the guide from a clean clone reaches a passing `pnpm test` and a running app.
- CI passes on a pull request with lint, typecheck, unit, integration and e2e; the concurrency job runs separately.
- A search shows no leftover reference to the reference project's product in tracked files.

## Implementation Notes

- `PROJECT.md` answers "what is this project and how do I work on it": stack, structure, commands, conventions, environment variables, testing layers, links to PRD, tasks and ADRs.
- Domain knowledge stays in `CONTEXT.md` and ADRs, not in `PROJECT.md`.
- Root `.gitignore` already excludes the reference project; confirm nothing under it is tracked.

## Dependencies

T9.

## Completion Signal

A newcomer can clone, follow `PROJECT.md`, and get a green test run; CI is green.
