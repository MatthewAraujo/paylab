# F12 context — accessibility, responsive polish, browser smoke, documentation

Spec: `../tasks/F12.md`. Inputs: F1–F11 summaries, `../PRD.md` US-91..96, `playwright.config.ts`, `src/app/globals.css`. Constraints: no real API or Docker, financial routes untouched, no `git stash`/`clean`/`reset --hard`.
