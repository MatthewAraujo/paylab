# F12 — Accessibility, responsive polish, browser smoke, and documentation

## Objective

Verify and finish the area as a whole: accessibility, responsive behavior, a browser smoke test, and up-to-date handbooks.

## Acceptance Criteria Served

US-91..96 and end-to-end validation of the rest.

## Affected Files / Areas

- Benchmark components (polish only), `tests/e2e/`, `PROJECT.md`, `CONTEXT.md`, `.env.example` comments, and this plan's records.

## Test-First Plan

- Playwright smoke test with request interception and typed fixtures: navigation to Benchmarks, the latest Run, comparison selection, Run detail, an Artifact opening, and a configured API base URL. A manual check against a real local API with the imported evidence is written down.
- Automated checks that status and change meanings have text, that every interactive element is keyboard reachable with visible focus, that dialogs restore focus, and that reduced motion disables animation.
- Manual verification at 1440, 1024, and 390 pixels and at 200% zoom: no clipped active navigation label, scroll regions labelled and focusable, long identifiers and log lines handled, secondary text at the readable baseline.

## Implementation Notes

- Document the new capability in `PROJECT.md` and the canonical terms and decisions in `CONTEXT.md` without duplicating each other; note the browser-direct read decision, the local-only boundary, and the zero-reference divergence.
- Run the full gate set and record every exit code. Do not broaden visual changes to financial routes.

## Dependencies

F1 to F11.

## Completion Signal

`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, and `pnpm test:e2e` pass; the smoke test runs; the manual checklist is recorded; documentation is current.
